<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="foundtruck.com.br.foundtruck"
    android:screenOrientation="portrait">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.InicioActivity"
            android:label="@string/title_activity_inicio" />
        <activity android:name=".activities.CadastroActivity" />
        <activity
            android:name=".activities.test"
            android:label="@string/title_activity_test"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.CustomizedAndroidListActivity"
            android:label="@string/title_activity_customized_android_list"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>